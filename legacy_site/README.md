# Legacy Static Site Archive

This folder contains archived files from the previous static HTML/PHP portfolio implementation.

The active website is now generated by `unified_docs` + MkDocs from the repository root.
